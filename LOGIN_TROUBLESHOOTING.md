# 로그인 문제 해결 가이드

## 🔍 로그인 오류 진단

### 1단계: 브라우저 콘솔 확인

1. **F12** 또는 **Cmd+Option+I** (Mac)로 개발자 도구 열기
2. **Console 탭** 클릭
3. 로그인 시도 시 다음 메시지들을 확인:
   - `[SUPABASE LOGIN] 로그인 시도`
   - `[SUPABASE LOGIN] 로그인 오류:` (오류가 있는 경우)
   - 오류 메시지의 전체 내용 확인

### 2단계: 환경 변수 확인

브라우저 콘솔에서 다음을 실행:

```javascript
console.log('Supabase URL:', import.meta.env.VITE_SUPABASE_URL);
console.log('Has Key:', !!import.meta.env.VITE_SUPABASE_ANON_KEY);
```

**예상 결과:**
- `Supabase URL: https://your-project.supabase.co`
- `Has Key: true`

**문제가 있는 경우:**
- URL이나 Key가 `undefined`이면 `.env.local` 파일을 확인하세요
- 개발 서버를 재시작하세요 (`npm run dev`)

### 3단계: 일반적인 로그인 오류

#### 오류 1: "Invalid login credentials"
**원인**: 이메일 또는 비밀번호가 올바르지 않음

**해결**:
1. 이메일 주소가 정확한지 확인
2. 비밀번호가 정확한지 확인
3. Supabase 대시보드에서 사용자가 존재하는지 확인

#### 오류 2: "Email not confirmed"
**원인**: 이메일 인증이 완료되지 않음

**해결**:
1. Supabase 대시보드 → Authentication → Users
2. 해당 사용자의 이메일 인증 상태 확인
3. 필요시 수동으로 인증 완료 처리

#### 오류 3: "Too many requests"
**원인**: 너무 많은 로그인 시도

**해결**:
1. 몇 분 기다린 후 다시 시도
2. Supabase 대시보드에서 Rate Limit 설정 확인

#### 오류 4: "Failed to fetch" 또는 "ERR_NAME_NOT_RESOLVED"
**원인**: Supabase 서버에 연결할 수 없음

**해결**:
1. 네트워크 연결 확인
2. Supabase 프로젝트가 일시 중지되지 않았는지 확인
3. `.env.local` 파일의 URL이 올바른지 확인

#### 오류 5: "supabaseUrl is required"
**원인**: Supabase URL이 설정되지 않음

**해결**:
1. `.env.local` 파일 확인
2. `VITE_SUPABASE_URL`이 올바르게 설정되어 있는지 확인
3. 개발 서버 재시작

### 4단계: Supabase 사용자 확인

1. **Supabase 대시보드** 접속
2. **Authentication → Users** 메뉴로 이동
3. 로그인하려는 이메일이 존재하는지 확인
4. 사용자 상태 확인:
   - Email confirmed: ✅ (인증 완료)
   - User metadata: name, role 등 확인

### 5단계: 이름으로 로그인 시도

앱은 이름으로도 로그인할 수 있지만, 내부적으로는 이메일로 변환합니다.

**주의사항**:
- 이름으로 로그인하려면 `user_profiles` 테이블에 해당 이름의 사용자가 있어야 함
- RLS 정책 때문에 로그인 전에는 프로필 조회가 안 될 수 있음
- **가장 안전한 방법은 이메일로 로그인하는 것**입니다

### 6단계: 개발 서버 재시작

환경 변수를 변경한 경우:

```bash
# 서버 중지 (Ctrl+C)
npm run dev
```

### 7단계: 브라우저 캐시 삭제

1. **강제 새로고침**: Cmd+Shift+R (Mac) 또는 Ctrl+Shift+R (Windows)
2. 또는 **브라우저 캐시 완전 삭제** 후 다시 접속

## 📝 체크리스트

- [ ] `.env.local` 파일에 `VITE_SUPABASE_URL`과 `VITE_SUPABASE_ANON_KEY`가 설정되어 있음
- [ ] 개발 서버가 재시작됨
- [ ] 브라우저 콘솔에서 환경 변수가 로드되었는지 확인
- [ ] Supabase 대시보드에서 사용자가 존재함
- [ ] 사용자의 이메일이 인증됨
- [ ] 비밀번호가 올바름
- [ ] 네트워크 연결이 정상임
- [ ] Supabase 프로젝트가 활성 상태임

## 🆘 문제가 계속되면

1. **브라우저 콘솔의 전체 오류 메시지** 복사
2. **Network 탭**에서 Supabase 요청 상태 확인
3. **Supabase 로그** 확인 (Settings → Logs)
4. **Supabase 대시보드**에서 사용자 상태 확인

## 💡 디버깅 팁

브라우저 콘솔에서 다음을 실행하여 상세 정보 확인:

```javascript
// Supabase 클라이언트 확인
import { supabase } from './src/utils/supabase';
console.log('Supabase client:', supabase);

// 현재 세션 확인
supabase.auth.getSession().then(({ data, error }) => {
  console.log('Current session:', data);
  console.log('Session error:', error);
});

// 사용자 정보 확인
supabase.auth.getUser().then(({ data, error }) => {
  console.log('Current user:', data);
  console.log('User error:', error);
});
```

